/*** imports ***/
import { PipeStage } from "../core/types"

/*** stage-handler ***/
export const rateLimiter = (): PipeStage<void> => {
    return {
        handler: async (ctx) => {

        }
    }
}